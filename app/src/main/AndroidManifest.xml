<?xml version="1.1" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <!-- Allow querying all launcher apps for app name resolution -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- Allow querying TTS services (required for Android 11+ and especially Android 15) -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <!-- Permission to access notifications -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions" />
    
    <!-- Permission for Quick Settings tiles (Android 7.0+) -->
    <uses-permission android:name="android.permission.BIND_QUICK_SETTINGS_TILE"
        tools:ignore="ProtectedPermissions" />
    
    <!-- Accessibility Service Permission (OPTIONAL FEATURE) -->
    <!-- 
    PURPOSE: This permission enables advanced user control features for the notification reader.
    
    SPECIFIC USES:
    - Volume button control: Allow users to stop/pause notification readouts using hardware volume buttons
    - Enhanced gesture recognition: Provide alternative control methods beyond current shake/wave detection
    - Global media control: Better integration with music apps and media playback
    - System-wide shortcuts: Enable quick actions from anywhere in the system
    
    PRIVACY & SECURITY:
    - This permission is COMPLETELY OPTIONAL - core app functionality works without it
    - All processing stays on-device - no data collection or transmission
    - Users must explicitly enable this in Android's accessibility settings
    - Each feature can be individually enabled/disabled by the user
    - No access to personal data, passwords, or sensitive information
    
    FOSS COMPLIANCE:
    - Permission is only used for legitimate accessibility features
    - No tracking, analytics, or data collection
    - Transparent about what each feature does
    - Users have full control over all functionality
    - Can be completely disabled without affecting core app features
    
    This permission follows the same privacy-first approach as the existing notification listener permission.
    -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"
        tools:ignore="ProtectedPermissions" />
    
    <!-- Permission to post notifications (required for Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Permission to use text-to-speech -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    
    <!-- Permission for foreground service media playback (Android 14+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />



    <!-- Permission for auto-start on boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Permission to request battery optimization exemption -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Permission for external storage (for Android < API 29) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Permission to access the internet for downloading network voices -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permission to detect phone calls for Honour Phone Calls feature -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Conditional filtering permissions -->
    <!-- Location is not used by the app, but is required for Bluetooth scanning -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />
    <application
        android:name=".SpeakThatApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpeakThat"
        tools:targetApi="31">

        <!-- Onboarding Activity -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="false" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Settings Activities -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".SupportActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".GeneralSettingsActivity"
            android:exported="false" />

        <activity
            android:name=".settings.BehaviorSettingsActivity"
            android:exported="false" />



        <activity
            android:name=".RulesActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

                    <activity
                android:name=".RuleBuilderActivity"
                android:exported="false"
                android:parentActivityName=".RulesActivity" />

                    <activity
                android:name=".TemplateSelectionActivity"
                android:exported="false"
                android:parentActivityName=".RulesActivity" />

            <activity
                android:name=".rules.TriggerConfigActivity"
                android:exported="false"
                android:parentActivityName=".RuleBuilderActivity" />

            <activity
                android:name=".rules.ActionConfigActivity"
                android:exported="false"
                android:parentActivityName=".RuleBuilderActivity" />

            <activity
                android:name=".rules.ExceptionConfigActivity"
                android:exported="false"
                android:parentActivityName=".RuleBuilderActivity" />

            <activity
                android:name=".BluetoothConditionActivity"
                android:exported="false"
                android:parentActivityName=".RulesActivity" />

        <activity
            android:name=".VoiceSettingsActivity"
            android:exported="false" />

        <activity
            android:name=".FilterSettingsActivity"
            android:exported="false" />

        <activity
            android:name=".AppPickerActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />



        <activity
            android:name=".NotificationHistoryActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".DevelopmentSettingsActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <activity
            android:name=".TestSettingsActivity"
            android:exported="false"
            android:parentActivityName=".GeneralSettingsActivity" />

        <activity
            android:name=".WaveCalibrationActivity"
            android:exported="false"
            android:parentActivityName=".settings.BehaviorSettingsActivity" />

        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- SelfTest Activity -->
        <activity
            android:name=".SelfTestActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Update Activity -->
        <activity
            android:name=".UpdateActivity"
            android:exported="false"
            android:parentActivityName=".AboutActivity" />

        <!-- Notification Listener Service -->
        <service
            android:name=".NotificationReaderService"
            android:label="@string/service_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Foreground host for the persistent notification indicator -->
        <service
            android:name=".PersistentIndicatorService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Quick Settings Tile Service -->
        <service
            android:name=".SpeakThatTileService"
            android:label="@string/app_name"
            android:icon="@drawable/logo_speakthat"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.ACTIVE_TILE"
                android:value="true" />
        </service>

        <!-- Accessibility Service -->
        <service
            android:name=".SpeakThatAccessibilityService"
            android:label="@string/accessibility_service_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- BroadcastReceiver for automation intents (enabled when External Automation mode is active) -->
        <receiver
            android:name=".automation.AutomationBroadcastReceiver"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="com.micoyc.speakthat.intent.ACTION_ENABLE_SPEAKTHAT" />
                <action android:name="com.micoyc.speakthat.intent.ACTION_DISABLE_SPEAKTHAT" />
            </intent-filter>
        </receiver>

        <!-- Boot Receiver for auto-start functionality -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing exported files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.micoyc.speakthat.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest> 
