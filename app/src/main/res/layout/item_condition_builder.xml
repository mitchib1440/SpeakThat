<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/rounded_background"
    android:padding="16dp"
    android:layout_marginBottom="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="12dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/item_condition_builder_when"
            android:textColor="?android:attr/textColorSecondary"
            android:textSize="14sp" />

        <Spinner
            android:id="@+id/spinnerConditionType"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginHorizontal="8dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnRemoveCondition"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/item_condition_builder_remove"
            android:textColor="?attr/textColorTertiary"
            android:textSize="18sp"
            android:textStyle="bold"
            android:minWidth="40dp"
            android:minHeight="40dp"
            app:backgroundTint="@color/red_200"
            app:strokeColor="@color/red_200"
            app:strokeWidth="2dp"
            app:cornerRadius="20dp"
            android:layout_marginStart="8dp" />

    </LinearLayout>

    <!-- Parameters will be dynamically added here -->
    <LinearLayout
        android:id="@+id/parametersContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" />

</LinearLayout> 